python-telegram-bot[job-queue]==22.7
python-dotenv==1.0.0
openpyxl==3.1.5
Pillow>=10.0.0

# Xatolarni kuzatish — faqat SENTRY_DSN .env'da o'rnatilsa faollashadi
sentry-sdk>=2.0

# PostgreSQL — faqat DB_BACKEND=postgres bo'lganda kerak (Faza 0: SQLite->PG ko'chirish)
psycopg[binary]>=3.1
